function isAnagramOfPalindrome(str) {
    const charCount = {};
    let oddCount = 0;
  
    for (let i = 0; i < str.length; i++) {
      const char = str[i];
      charCount[char] = (charCount[char] || 0) + 1;
    }
}
